import { TwoDecimalNegativeInput } from './two-decimal-negative-input';

describe('TwoDecimalNegativeInput', () => {
  it('should create an instance', () => {
    const directive = new TwoDecimalNegativeInput();
    expect(directive).toBeTruthy();
  });
});
