import { TwoDecimalInput } from './two-decimal-input';

describe('TwoDecimalInput', () => {
  it('should create an instance', () => {
    const directive = new TwoDecimalInput();
    expect(directive).toBeTruthy();
  });
});
