import { TwoDecimalValidator } from './two-decimal.validator';

describe('TwoDecimalValidator', () => {
  it('should create an instance', () => {
    expect(new TwoDecimalValidator()).toBeTruthy();
  });
});
