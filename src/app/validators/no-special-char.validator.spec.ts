import { NoSpecialCharValidator } from './no-special-char.validator';

describe('NoSpecialCharValidator', () => {
  it('should create an instance', () => {
    expect(new NoSpecialCharValidator()).toBeTruthy();
  });
});
